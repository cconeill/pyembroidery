Fix the bug described in: $ARGUMENTS

Instructions:
1. Read the relevant file(s) and understand the bug
2. Implement the fix with minimal changes — do not refactor surrounding code
3. Add `from __future__ import annotations` if modifying a file that lacks it
4. Run the full test suite: `python -m unittest discover -s test -t .`
5. All 232 tests must pass
6. If the fix is tracked in CLAUDE.md, update the checkbox (change `[ ]` to `[x]`)
7. Commit with a descriptive message explaining what was fixed and why
